'use strict';

describe('Domani.table module', function() {

  beforeEach(module('Domani.tableView'));

  describe('table controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var tableCtrl = $controller('tableCtrl');
      expect(tableCtrl).toBeDefined();
    }));

  });
});